{
  "name": "demoweave-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Agent-native documentation studio for software repositories",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "clean": "pnpm -r --if-present clean"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
